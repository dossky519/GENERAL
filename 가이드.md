# 사용 가이드 — 이 시스템으로 정보를 얻는 법

이미 `data/portfolio.md`(보유 현황), `data/watchlist.md`(관심 종목)에는 실제 데이터가 들어가 있습니다. **두 파일은 `.gitignore` 처리되어 있어 이 컴퓨터/세션에만 존재하고 공개 저장소에는 올라가지 않습니다** — 형식만 궁금하면 `data/portfolio.example.md`, `data/watchlist.example.md`를 보세요. 이제부터는 아래 문구들을 그대로 Claude Code 세션에 입력하기만 하면 됩니다. 모든 결과는 `.claude/skills/investing-principles.md`의 5원칙(출처·계산·사실해석분리·매매신호금지·1차스크리너)을 따릅니다.

## 0. 하루 흐름 한눈에

```
아침: "오늘 브리핑"
   │
   ├─ 액션 신호 없음 → 끝. 오늘은 안 봐도 됨.
   │
   └─ 액션 신호 있음 (예: "AAPL 실적 D-3 → story-reader 권장")
        │
        ├─ "AAPL 스토리 분석해줘"      → 지난 2~3년 변화 확인
        ├─ "AAPL 지금 사도 되나?"      → 역DCF로 갭 확인
        └─ "AAPL 분석해줘"            → 처음 보는 종목이면 한 장 요약부터

주 1회 정도: "포트폴리오 점검해줘"    → 집중도·중복노출·환율노출 점검
필요할 때만: "갱신해줘"               → 브리핑 + 대시보드 HTML 최신화
```

## 1. 매일 — 브리핑 받기

**입력:** `오늘 브리핑`

**하는 일:** `data/portfolio.md`의 보유 종목 밤사이 움직임, 환율·매크로, `data/watchlist.md`의 관심 테마 뉴스, 다가오는 실적 일정을 훑고 "오늘의 액션 신호"를 알려줍니다. 신호가 없으면 "오늘은 특별한 신호 없음"이라고 정직하게 답합니다 — 매일 뭔가 나와야 하는 건 아닙니다.

**저장 위치:** `output/briefs/YYYY-MM-DD.md`

## 2. 신호를 받으면 — 온디맨드 딥다이브

브리핑이 "이 종목 더 파봐"라고 신호를 주면, 상황에 맞는 스킬을 호출합니다.

| 상황 | 입력 문구 예시 | 뭘 알려주나 |
|---|---|---|
| 처음 보는 종목/테마를 빠르게 이해하고 싶을 때 | `NVDA 분석해줘` | 돈 버는 구조, 매출 구성, 업종 핵심지표, 재무 스냅샷 (한 장 카드) |
| 최근 실적/공시에서 뭐가 달라졌는지 궁금할 때 | `SCHD 스토리 분석해줘` | 연도별 문구 변화, 가이던스 대비 실제, 어닝콜 톤 추적 |
| 지금 가격이 부담스러운지 궁금할 때 | `AAPL 지금 사도 되나?` (예시 티커) | 역DCF로 "이 가격이 요구하는 성장률" vs 과거 실제 성장률 갭 |

**10-K/사업보고서 PDF가 있다면** 업로드 후 같은 문구를 쓰면 페이지 출처가 붙는 정밀 모드로 전환됩니다.

**저장 위치:** `output/cards/[티커]_[날짜].md`

## 3. 포트폴리오 전체 점검 (주 1회 정도 권장)

**입력:** `포트폴리오 점검해줘`

**하는 일:** `data/portfolio.md`에 등록된 보유 종목 전체를 놓고 집중도(HHI), ETF를 경유한 숨은 중복 노출, 자산유형별(지수 ETF vs 커버드콜 인컴 ETF 등) 평가 기준 차등 적용, 환율 노출도를 계산합니다. 커버드콜/옵션인컴 계열 ETF 비중이 크면, 배당수익률만이 아니라 NAV 추이·자본잠식 여부까지 짚어줍니다.

## 4. 관심 종목 뉴스 (필요할 때)

`data/watchlist.md`에 등록해둔 티커들은 `오늘 브리핑`을 돌릴 때 자동으로 같이 훑입니다. 특정 종목만 바로 확인하고 싶으면 `[티커] 분석해줘` 형태로 개별 호출하면 됩니다.

## 5. 대시보드가 필요할 때

**입력:** `갱신해줘`

**하는 일:** 최신 브리핑 + `output/dashboard.html`을 함께 갱신합니다. 자동으로 매일 도는 게 아니라 이 문구를 칠 때만 동작합니다(API 비용/구독 한도 절약 목적). 갱신 후 로컬 서버로 실제 렌더링까지 확인한 뒤 완료 보고합니다.

## 6. 데이터가 바뀌었을 때 (매매·관심종목 변경)

이 시스템은 계좌 연동을 하지 않으므로, 아래는 직접 알려줘야 반영됩니다.

- 매수/매도, 수량 변경: `"포트폴리오에서 [티커] 수량/금액 [값]으로 업데이트해줘"` 라고 말하면 `data/portfolio.md`를 갱신합니다.
- 관심 종목 추가/삭제: `"[티커] 관심종목에 추가해줘"` / `"[티커] 관심종목에서 빼줘"` 라고 말하면 `data/watchlist.md`를 갱신합니다.

## 7. 결과물은 어디서 보나

| 파일/폴더 | 내용 |
|---|---|
| `output/briefs/YYYY-MM-DD.md` | 그날그날 일일 브리핑 |
| `output/cards/[티커]_*.md` | 기업 해독·스토리·가격 판독 결과 |
| `output/cards/portfolio_*.md` | 포트폴리오 콕핏 결과 |
| `output/dashboard.html` | 대시보드 (갱신해줘로 최신화) |

## 8. 이 시스템이 절대 하지 않는 것 (재확인)

- "사라/팔아라" 같은 매매 신호를 주지 않습니다. 항상 "어디를 더 파봐야 하는지"까지만 제시합니다.
- 출처 없는 숫자를 만들어내지 않습니다. 모르면 "확인 필요"라고 답합니다.
- 결론은 투자 자문이 아닙니다 — 실제 매매에 쓰는 숫자는 원문에서 직접 한 번 더 확인해야 합니다.

## 9. 자주 헷갈리는 것

- **"분석해줘" vs "스토리 분석해줘" vs "지금 사도 되나?"** — 셋 다 다른 스킬입니다. 처음 보는 회사면 "분석해줘"(기업 해독기)부터, 이미 아는 회사의 최근 변화가 궁금하면 "스토리 분석해줘", 가격이 부담스러우면 "지금 사도 되나?"를 씁니다.
- **왜 매일 대시보드가 자동으로 안 갱신되나요?** — 의도된 설계입니다. "갱신해줘"라고 부를 때만 동작해서 불필요한 웹검색/API 호출을 막습니다.
- **집중도 점검을 매일 안 해도 되나요?** — 네. 포트폴리오 구성은 매일 크게 안 바뀌므로 주 1회 정도가 적당합니다. 매수/매도 후에는 그때그때 돌려도 됩니다.
